---
agent: speckit.implement
---

Requirements:

- Follow all rules defined in `constitution.md`.
- Follow the architecture and technical decisions defined in `plan.md`.
- After completing the implementation:
  - Commit the code changes.
  - Mark the task as done in `tasks.md`.
